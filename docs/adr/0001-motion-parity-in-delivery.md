# Preserve movement in delivery

When a Project uses a Movement pattern, its Preview must render equivalently into multichannel WAV and into time-varying ADM-BWF object metadata. Preview-only motion and static ADM object metadata were rejected because the product's production-ready spatial mix promise requires movement to survive delivery.
