# Deliver immutable collaborative Project snapshots

Project collaborators receive direct owner, editor, or viewer membership and see live ordinary edits with revision-conflict detection. Each Delivery job renders its immutable Delivery snapshot and retains a Restore point, rather than silently rendering a later collaborator edit; organization-wide ownership and branching are deferred.
