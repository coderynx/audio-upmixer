# Use authenticated private cloud workspaces

The public cloud product will give each Producer account an authorized Personal workspace, retain Project assets until Recoverable deletion expires, and run Managed delivery jobs asynchronously. Anonymous access, shared-by-URL ownership, and use of Private audio for model training without explicit opt-in were rejected because managed processing and durable storage require a clear producer trust boundary.
