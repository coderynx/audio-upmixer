# Ship a self-hostable cloud reference deployment

The cloud platform will be delivered as a supported Reference deployment that its operator hosts, rather than as a shared service operated by this project. This preserves the open-source distribution model while still requiring authenticated Personal workspaces and Private audio handling in every deployment.
