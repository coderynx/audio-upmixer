# CLI

The command-line context turns explicit user input into reproducible core runs.

## Language

**Manifest run**: A batch of asset jobs resolved from one manifest.
_Avoid_: project

**Override**: A CLI value that wins over manifest and profile values.
_Avoid_: default
